export default function DevelopersPage() {
  return (
    <main className="page-shell workspace-layout">
      <section className="hero-card">
        <p className="eyebrow">Developer Surface</p>
        <h1 className="workspace-heading">开放接口方向</h1>
        <p className="workspace-subtitle hero-copy">
          这里会承接 API Key 管理、知识域授权和 `POST /api/v1/rag/chat` 的开发者入口。当前页面用于稳定外部能力入口的前端路径。
        </p>
      </section>
    </main>
  );
}
